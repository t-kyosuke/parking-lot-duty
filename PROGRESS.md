# PROGRESS.md - 進捗管理

## 2026-07-01 セッション記録
### 実施内容
- **カゴ表示を「持ち帰り」基準に変更（表示のみ・割り当てロジックは一切不変）**：塚原さんの「練習/試合のたびに“誰がカゴを持ち帰るか”を分かりやすく」という意図に対応。従来の「その日カゴを持ってくる人（`kagoCoach`）」表示を、「**その日の練習後に持ち帰る人＝次のカゴ利用日の担当者**」表示へ変更
- 純粋関数 **`computeKagoTakeHome(assignments)`** を新設（`assignParking.ts`）。日付順で次のカゴ利用日の担当者を持ち帰り人として返す。月内最後は「翌月へ引き継ぎ」、次が要確認なら「要確認（今カゴ: ◯◯さん）」。公開画面/LINE/管理画面の3面すべてに反映
- 公開画面ラベルを**8文字で統一**：「🅿️当日の駐車場当番／🎥当日のビデオ当番／🧺当日カゴ持ち帰り」。矢印はCSSの `::after` で **PC＝▶／スマホ（≤600px）＝▼** に自動切替。スマホは `duty-line` を縦積みにして横あふれ回避。上部バナー文言も新仕様へ差し替え
- 管理画面（塚原さん用）は編集対象を従来どおり「🧺カゴを持ってくる人▶」（＝`kagoCoach`）に保ちつつ、下に読み取り専用で「→終わりに◯◯さんが持ち帰り」を補足表示（`kagoCell` をフラグメント化）
- **設計はAskUserQuestionの具体プレビューで都度合意してから実装**（見せ方3案→反映範囲→ラベル案→矢印まで段階確認）
- **トリプルチェック実施**：6観点の並行レビュー＋各指摘の敵対的裏取り＋test/build/lint（計8エージェント）
- **本番デプロイ2回**：`b3a8da4`（表示変更本体）／`774fa81`（下記の旧データ修正）を main へプッシュ → GitHub Actions 自動デプロイ成功、本番JS（`index-BAh4Clk0.js`）反映を確認
### 検証
- テスト：`computeKagoTakeHome` の性質テストを**6件追加（旧フォーマット1件含む）＝合計30件全通過**。`npm run build` 型エラー0
- ブラウザ実機で PC（横並び・▶）／スマホ375px（縦積み・▼・横あふれ無し）、公開画面・LINE文面・管理画面の3面、旧データ月（4月＝カゴ行なし）・新データ月（7月＝毎日カゴ持ち帰り＋末尾「翌月へ引き継ぎ」）をすべて確認
- CLAUDE.md を新仕様（表示改定・ラベル・矢印・件数30・旧データフォールバック）に更新
### 発生・解決したエラー
- **トリプルチェックで実バグ1件を発見・解決**：`computeKagoTakeHome` が**カゴ連鎖導入前（新フラグ無し）の旧・確定月データ（本番の4〜6月）**で誤表示（試合日の担当が消え「未定」に退行、無関係な練習日に他人名が「持ち帰り」表示、他は全て「未定」）。→ **旧フォーマット検出＋従来表示フォールバック**を `computeKagoTakeHome` 冒頭に追加（`kagoCarriedByParking`/`kagoNeedsConfirm` フラグの有無で判定。旧データは「その日のカゴ担当のみ表示・担当なしの日はカゴ行を出さない」へ）。本番4〜6月の誤表示も解消。性質テスト1件追加＋既存テスト用 `mk` ヘルパーに新フラグ既定値を付与
- preview検証で月送り（◀▶）クリックが同一eval内では即反映されず前の月が見えた → 別evalで再取得して確認（React再レンダーの非同期・既知事象）
- lint 9件（エラー5・警告4）は大半が既存分＋LINE整形に必須の全角スペース（意図的。親コミットにも同数存在）。デプロイは `npm run build` のみで走るため影響なし＝今回は未対応
### 次回の課題
- 8月以降の実データCSVで運用。旧フォーマット月（4〜6月）は「当番を決定する」で再割り当てすれば新方式データに更新され、必要なら「毎回カゴ持ち帰り」表示に揃う（現状は旧データなのでカゴ行なし＝正しい挙動）
- 今回の変更は**表示のみ・`git revert` で戻せる**（復元ポイント＝改定直前コミット `36e452f`）。割り当てロジック（`assignKagoChain` 等）は無改修
- 任意対応：既存lint（全角スペース等）の整理、`deploy.yml` の Node.js 20 非推奨警告（GitHub側がNode24へ自動振替中で実害なし）
---

## 2026-06-22 セッション記録
### 背景（塚原さんとの設計対話で確定した要件）
- カゴ（黄色うちわ＋道具が入った“物理的に1個”の物）の運び役を、**試合日だけでなく「カゴが必要な全セッション（土日祝の練習＋試合）」**で決めたい。今までは善意（次の駐車場当番が持ち帰り、欠席なら誰かが善意で）任せで、特に**土曜練習の持ち込みが未決**だった
- カゴは「前回来ていた人」しか次に持って来られない＝**バトンリレー**。これを筋を通して自動化する
- **最優先**：駐車場・ビデオの公平さは絶対に崩さない（カゴの都合で当番回数が偏らせない）。日曜は「駐車場当番＝カゴ運び」のセットは維持（うちわが入っているため）。ただし**一方通行**（駐車場/ビデオを先に公平に決め、カゴは後追い）にすることで両立
- 合宿は合宿係が他の荷物と運ぶので連鎖から除外でOK
- **「戻せるように」を最優先要望**（超重要）→ 着手前に①現仕様スナップショット `docs/SPEC-SNAPSHOT-before-saturday-kago.md` ②復元ポイント git tag `pre-saturday-kago`（コミット `13808f2`）を作成
### 実施内容（カゴ連鎖 / バトンリレー方式）
- `assignParking.ts`：`assignKagoDuties`（試合日のみ）を廃止し **`assignKagoChain`** を新設。全カゴ必要セッションを日付順に処理し、各日の運び役を「**今日◯ ∩ 前回◯**」から累計最少・連続回避・同日の駐車場/ビデオ回避で選ぶ。日曜で駐車場当番が前回◯ならそのまま運ぶ（`kagoCarriedByParking=true`・**数えない**）。候補ゼロは**要確認**（`kagoNeedsConfirm`＋`kagoHolder` で現保持者表示）で連鎖は据え置き継続。**`assignDuties`/`pickByCount` は無改修＝一方通行**
- 型：`AssignmentResult` に `kagoCarriedByParking` / `kagoNeedsConfirm` / `kagoHolder` を追加（旧データは optional で後方互換）。`isKagoCounted()` を新設（カゴ係指名分だけ累計対象）
- `storage.ts`：`getPreviousKagoSession`（前月最後のカゴ・セッションの保持者＋出欠＝月またぎ引き継ぎ）を新設。`recalculateCumulativeCounts`/`getCountsForAssignment` のカゴ集計を `isKagoCounted` ベースへ（兼任・要確認は数えない／旧の試合日カゴは従来どおり数える後方互換）
- `AdminView.handleAssign`：駐車場/ビデオを先に確定 → カゴ必要全セッション（練習/運動会等/試合、合宿・休み除外）を日付順にまとめ、前月引き継ぎ `seed` 付きで `assignKagoChain` を1回実行する2段構成へ
- 画面・LINE：練習日（土曜含む）にもカゴ行を表示（日曜の兼任日は出さない）。要確認は「🧺カゴ▶要確認（今カゴ: ◯◯さん）」を表示（AssignmentResult/PublicView/LineAnnouncement・`.kago-needs-confirm` スタイル追加）。手動でカゴ係を選ぶと指名扱い（数える）に
- **マイグレーション不要**（過去の確定・公開済み月は勝手に書き換えない。新ルールはこれから割り当てる月から）
- **本番デプロイ実施**（塚原さんのGO後）：main へ2コミット（`840ff38` カゴ連鎖本体／`fccd180` トリプルチェック軽微修正）をプッシュ → GitHub Actions 自動デプロイ成功。本番JSに新機能・新文言の反映を確認
- **多角的トリプルチェック実施**（5観点×16エージェント・各指摘を逆検証）：機能バグ・機能的ヌケモレ0。確定した軽微指摘（旧「試合日のみ」前提のUI文言ほか）を反映 → 出欠プレビュー/累計チャート/設定のカゴ文言を全セッション仕様へ統一、`getPreviousKagoSession` の新旧フォーマット判定を新フラグ有無で正確化、`getPreviousLastCoach` の未使用 `'kago'` 分岐を削除（いずれも動作ロジック不変）
### 検証
- **テスト**：`assignKagoChain` 性質ベース11件＋公平性シミュレーション1件に刷新。**合計24件全通過**（`npx vitest run`）
- **公平性シミュレーション（1年・出席80%・構造的偏り＝土曜サブグループ/遠征常連）**：80セッションで**要確認0回**・カゴ係指名の最多−最少差**＝3**（駐車場兼任で運んだ24日は除く）。連鎖が破綻せず十分公平
- `npm run build` 型エラー0。ブラウザ実機（6月にサンプル投入）で表示5パターン（土曜カゴ▶／日曜は兼任で行なし／要確認＋現保持者／⚽試合カゴ▶／日曜フォールバックカゴ▶）をスナップショットで確認・**コンソールエラー0件**
- 軽微修正後も再テスト24件・build OK。本番デプロイ完了（completed/success）・本番JSハッシュ更新（`index-DBLiNPCg.js`）・旧文言「カゴ当番（試合日）」残存0を確認
### 発生・解決したエラー
- ブラウザ検証で preview の初期ページが `data:` URL のため localStorage が使えずエラー → 先にアプリURL（`/parking-lot-duty/`）へ移動してから流し込んで解決（既知事象）
- 本番ビルドで `pickKagoCarrier` の未使用引数 `order` による TS6133 → 引数を削除して解決（再ビルド成功）
- リモートURLにアクセストークンが平文埋め込み（既存設定）。push 出力は `sed` で伏字にして扱い、再表示しないよう注意
### 次回の課題
- 塚原さんが実データCSV（土曜練習を含む月）で割り当て → 土曜カゴの実運用デビュー。実運用後に「やっぱり前の方が…」となれば tag `pre-saturday-kago` へ戻して再ビルド・再公開（手順は `docs/SPEC-SNAPSHOT-before-saturday-kago.md` 末尾）
- 公平性は「カゴ係指名のみ」で均す設計。日曜兼任が続くと特定者のカゴ累計が動かない点は許容範囲と判断（重い当番＝駐車場/ビデオの公平を優先のため）。気になれば設定の累計調整で微調整可
- 手動でカゴ係を途中変更した日の「下流の保持者・要確認表示」は次回フル割り当てで整う設計（累計・公平性には無影響）。気になる頻度になれば将来 pin 付き再連鎖を検討
- （任意・非緊急）GitHub Actions の Node20 非推奨警告 → actions を新しめのバージョンへ上げると安心
---

## 2026-06-16 セッション記録
### 実施内容
- **試合日の「カゴ当番」自動割り当て機能を新規追加**（駐車場・ビデオに続く3つ目の当番）
  - カゴ当番＝試合前の最後の練習でカゴを預かり、試合当日に試合会場へ持っていく役割。これまで善意・感覚で回していたものを公平に自動化
  - 確定した運用方針（本人確認済み）：①対象は駐車場・ビデオと同じ10名 ②カゴ単独でカウント（他の当番と独立）③対象は試合日（`type==='match'`）のみ
  - アルゴリズムは既存の「累計が少ない人を優先」を流用。新規 `assignKagoDuties`（`pickByCount` を再利用）を追加。出席◯の中から累計最少・同数は固定順先頭・直前のカゴ当番者は連続を避ける。曜日は問わない（土曜の試合でも割り当て）
  - `AssignmentResult` 型に `kagoCoach` と `isMatch` を追加。`assignDuties` の結果には常に `kagoCoach:null, isMatch:false` を付与
  - `AdminView.handleAssign`：試合日を抽出して `assignKagoDuties` を呼び、練習日＋試合日の結果を日付順に結合して保存
  - storage：`getKagoCounts`/`saveKagoCounts`、`KAGO_COUNTS='srs_kago_counts'` キー、`getCountsForAssignment`/`getPreviousLastCoach`/`recalculateCumulativeCounts` をカゴ対応、export/import にカゴ累計、変更履歴 `dutyType` に `'kago'` を追加
  - 画面：出欠プレビューに試合日の行（⚽）を追加（カゴ当番の出欠確認用）／割り当て結果は試合日に「⚽試合 🧺カゴ▶◯◯さん」／LINEアナウンスに「M月D日（曜）⚽試合：🧺カゴ▶◯◯さん」／閲覧画面の試合日に「🧺カゴ▶◯◯さん」／累計に「🧺カゴ当番」チャート／設定にカゴ累計の±調整を追加
  - **マイグレーション不要**（フィールド・カウントの追加のみで後方互換。古い保存データはカゴ欄が空になるだけ）
- **テスト**：`assignKagoDuties` の性質ベーステスト9件を追加（累計最少が選ばれる／同数は固定順先頭／連続しない／累計引き継ぎ／全員欠席はnull／候補枯渇で緩める／カゴ独自累計で選ぶ／試合日なしなら結果ゼロ／候補10名で林含まず）。既存12件は無改修で通過。**合計21件全通過**
- **検証**：`npm run test`（21件）・`npm run build`（型エラーなし）OK。ブラウザ実機で4月（4/26試合）の管理者画面＝結果/LINE/累計にカゴ表示・6月（6/14,6/28試合）の閲覧画面＝試合日に🧺カゴ表示を確認。コンソールエラー0件
- lint：今回の追加分に新規エラーなし（既存指摘＝App.tsx set-state／LineAnnouncement全角スペース／PublicView・AdminView useMemo依存はスコープ外で据え置き）
- 計画は `~/.claude/plans/concurrent-floating-sun.md` に記録・承認済み
- **多角的トリプルチェック実施**（5観点×指摘の独立再検証ワークフロー・エージェント9体）：機能・デプロイは問題なし（**本番JSがローカルビルドとバイト単位で一致**＝`cmp -s` で確認・型エラー0・クラッシュ経路なし）。確定した軽微な指摘4件を追加修正してデプロイ：
  - ① LINEアナウンスで「全員欠席でカゴ未定の試合日」が文面から消えていた → フィルタに `|| r.isMatch` を追加し「⚽試合：🧺カゴ▶未定」を必ず出力（結果・閲覧画面と一致）。ブラウザで実証
  - ② 出欠表の試合行に付けていた CSS クラス `.attendance-row-match` が未定義だった → App.css に薄黄＋黄ラインの強調を追加（◯/△/×セルの色は不変）
  - ③④ CLAUDE.md のテスト件数の取り残し（プロジェクト構成・現在の状態の「3件」）を実数 21 件に修正
  - 再テスト21件・ビルド・lint（新規ゼロ）・ブラウザ実証すべてOK
- **本番へ反映済み（デプロイ完了・確認済み）**：3コミット（`b3c8cc7` 機能本体／`9bed66d` 進捗記録／`aeee481` トリプルチェック修正）を main へプッシュ → GitHub Actions 自動デプロイ。本番JSが最終ローカルビルドとハッシュ一致（`index-EHvna1Dh.js`）＋本番JS内に新機能の文字列（🧺カゴ▶／カゴ当番（試合日）／⚽試合：🧺カゴ▶）を確認
### 発生・解決したエラー
- **git の index.lock 残骸でコミットがブロック**（`File exists`・`Apr 29` 付けの4/29セッション残骸）。稼働中gitプロセスが無いことを確認のうえ、git が表示する**絶対パス**（`.../.git/worktrees/<name>/index.lock`）を削除して解決。既知事象（メモリ `worktree-git-lock`）の再発
- ブラウザ検証用にlocalStorageへ確定済み月データを流し込む際、起動時の `migrateRemoveHayashi` が5月以降のassignmentsをクリアしてしまう点に注意。完了フラグ `srs_migration_remove_hayashi_v1='done'` を事前にセットして回避
- 2回目のブラウザ検証で preview の初期ページが `data:` URL のため localStorage が使えずエラー → 先にアプリURL（`/parking-lot-duty/`）へ移動してから流し込むことで解決
### 次回の課題
- 塚原さんが実データCSV（試合日を含む月）で割り当て → カゴ当番の実運用デビュー。これまでカゴをやってくれた実績を反映したい場合は設定画面「🧺 カゴ当番 - 累計回数の調整」で手動加算してから運用に入れる
- 過去に確定済みで試合日を含む月にカゴを入れたい場合は、その月を再度「当番を決定する」で再割り当て（二重カウントなし）
- 既存lint指摘（App.tsx set-state／LineAnnouncement全角スペース＝LINE整形の意図／useMemo依存）はスコープ外で据え置き。直す場合のみ別途
---

## 2026-05-31 セッション記録
### 実施内容
- **当番割り当てアルゴリズムを全面変更**：「固定順リストをポインタで循環」→「累計回数が少ない人を優先」方式へ
  - 基本：その日の出席者（◯）のうち累計最少の人を選ぶ。同数なら固定順（COACH_ORDER）の先頭
  - 連続防止：直前に当番した人は外す。候補が0人になる日は自動で緩める
  - 被り防止：同日に駐車場とビデオが同一人物にならない（従来どおり維持）
  - 駐車場＝日祝のみ／ビデオ＝全日（土含む）も従来どおり
- **データ構造を変更**：2ポインタ（owed/searchFrom）方式を廃止し、累計回数＋直前当番者ベースに
  - `storage.ts` に `getCountsForAssignment`（現在累計−当月確定分＝再割り当ての二重カウント防止＋手動調整の反映）と `getPreviousLastCoach`（選択月より前の確定済み月から直前当番者を取得）を追加
  - `AdminView.tsx` の `handleAssign` を新方式に。ポインタの保存処理を削除
- **UI調整**：設定画面の「次回開始位置」ドロップダウン（駐車場・ビデオ）を削除。累計調整に「回数を減らすと次に優先される」案内を追加。累計表示の「次回先頭候補」を「次に優先されやすい人（回数が最少）」に変更
- **月内上限ルールを追加**：累計0の人が単月で追いつき過ぎる（連続はしないが頻度が高くなる）のを防ぐため、同じ人は1か月に最大2回まで（`DEFAULT_MONTHLY_LIMIT=2`、駐車場・ビデオ各々）。全員が上限到達した日は自動で緩める
- **テストを性質ベースに作り直し**（12件全通過）：累計最少が選ばれる／同数は固定順先頭／連続しない／被らない／候補が尽きたら緩める／月内上限2回／全員上限なら緩める／土曜は駐車場なし／全員欠席は該当者なし／20日で差1以内 等。旧「期待出力との完全一致」テストは廃止
- **公平性を1年シミュレーションで検証**：出席率80%時の最多−最少差が 駐車場 2.0→**1.0**、ビデオ 3.0→**1.0** に改善。同日被り0件・連続当番0件
- **ブラウザ実機検証**：7月のテスト出欠で割り当て、回数最少の河井（駐車場0）・堀本（ビデオ0）が最優先・塚原松木（最多）は当番ゼロ・連続0件・被り0件を確認。上限導入で堀本のビデオが月3回→2回に収まることも確認
- **方針**：6月は旧方式の割り当てを維持（このまま）、7月分の割り当てから新方式を適用
- CLAUDE.md のアルゴリズム仕様・UI記述・localStorageキー表を新方式に更新
- **デッドコード整理**：新方式で不要化した未使用ポインタ関数4つ（単数形 get/saveParkingPointer・get/saveVideoPointer）を削除。`PointerState` 系は export/import 互換のため残置
- **本番へ反映済み**：2コミット（`d7c2afa` 本体＋`6a44fb1` 整理）を main へプッシュ → GitHub Actions 自動デプロイ完了。本番が参照するJS＝ローカルビルドのJSとハッシュ一致（`index-Blyrq3Up.js`・HTTP 200）を確認
- **トリプルチェック実施**：lint（変更した6ファイルに新規エラーなし）／test 12件／build／git未コミット0件・本番一致／本番JSハッシュ一致 をコマンド出力で実証
- ビルド成功・テスト12件全通過
### 発生・解決したエラー
- **git の index.lock 残骸でコミットが数回ブロック**（`File exists`）。worktree では作業ディレクトリ直下の `.git` がファイルのため相対パスの rm が空振りしていた。git がエラー表示する**絶対パス**（本体リポジトリ `.../parking-lot-duty/.git/worktrees/<name>/index.lock`、4/29 セッションの残骸）を削除して解決。メモリ `worktree-git-lock` に記録
- **ツール呼び出しのタグ書式ミスを多発**：`antml:invoke`/`antml:parameter` の接頭辞を落として `<invoke>` と書き "malformed" を連発した。原因は「説明文の直後にツールを書くと接頭辞を落とす」。対策＝**ツール呼び出しを返信の冒頭に置く**に切り替えて解消。メモリ `tool-tag-prefix-discipline` に記録
### 次回の課題
- 塚原さんが本番で7月の実データCSVをアップロードして新方式で割り当て → LINEアナウンス（新方式の実運用デビュー）
- 新しいコーチが当番に加わる際は、累計回数を既存メンバーの平均にセットしてから運用に入れる（自動化コードはなく、設定画面の累計調整で手動対応）
- 既存の lint 指摘3件（`App.tsx` の set-state-in-effect／`LineAnnouncement.tsx` の全角スペース／`PublicView.tsx` の useMemo 依存）は今回のスコープ外で未着手。直すなら LINE 出力の見た目を変えないか確認しながら
- 試合日の「カゴ当番」割り当て機能の実装（要件保留中・前回から継続）
---

## 2026-05-15 セッション記録
### 実施内容
- トップページ（閲覧者モード）のランダム背景画像を14枚→21枚に増設
  - 運動会・タッチフット大会の写真7枚を `public/hero-15.jpg`〜`hero-21.jpg` として追加
  - `src/components/PublicView.tsx` の `HERO_IMAGES` 配列長を `length: 14` → `length: 21` に変更
- スマホ表示でヒーローバナーの顔切れを修正
  - `src/App.css` の `.hero-banner` の `background-position` を `center 20%` → `center top` に変更
  - 横長バナー（約360×170px）に3:2比率の写真を `cover` で表示する際、顔（写真上部）が切れる問題への対応
  - 写真上部が常にバナー上端と揃うようになり、顔が切れなくなった（代わりに足元側がトリミングされる）
- ビルド成功・ユニットテスト11件全通過
- mainへpush → GitHub Actionsで自動デプロイ → 実機（スマホ）で見え方確認OK
### 発生・解決したエラー
- ワークツリーの index.lock（4/29のセッション残骸）が残っており `git reset HEAD` が失敗
  - 解決：古いロックファイル（`.git/worktrees/magical-sinoussi-ef9e94/index.lock`）を手動削除
- バナーの背景画像で顔が見切れる
  - 解決：上記のとおり `background-position` を `center top` に変更
### 次回の課題
- 試合日の「カゴ当番」割り当て機能の実装（要件保留中・前回から継続）
- 5月の駐車場・ビデオ当番を新しい10名構成で割り当てし、LINEへアナウンス（前回から継続）
---

## 2026-04-27 セッション記録
### 実施内容
- 林和憲を駐車場当番候補（COACH_ORDER）から削除し、ビデオと同じ10名構成に統一
- 除外コーチリスト（EXCLUDED_COACHES）に林さんを追加
- 起動時1回限りのマイグレーション処理 `migrateRemoveHayashi` を実装
  - 林さんの駐車場累計を削除
  - 5月以降の割り当て結果（assignments）をクリア
  - 駐車場ポインタを4月終了時点に巻き戻し
  - 完了フラグ `srs_migration_remove_hayashi_v1` で再実行防止
- ユニットテスト全11件パス（COACH_ORDER に林が含まれない検証アサーションを追加）
- CLAUDE.md / PROGRESS.md を10名前提に更新（旧記述の不整合も同時修正）
- mainへpush → GitHub Actions自動デプロイ完了
- 塚原さんのMacで「公開する」成功・5月CSV再アップロード可能な状態を確認
### 発生・解決したエラー
- 「data.json does not match {sha}」エラー（スマホ公開ボタン押下時）
  - 原因：ブラウザキャッシュにより GitHub API から古い SHA が返り、PUT 時にズレた
  - 解決：GET にキャッシュバスティングクエリ（`?_=timestamp`）と `cache: 'no-store'` を追加し、SHA不一致時の自動リトライを実装
- 「Failed to fetch」エラー（上記修正後に発生）
  - 原因：追加した `Cache-Control` / `If-None-Match` カスタムヘッダーが GitHub API の CORS プリフライトに弾かれた
  - 解決：当該カスタムヘッダーを削除し、キャッシュ回避はクエリパラメータと `cache: 'no-store'` のみで実現
### 次回の課題
- 試合日の「カゴ当番」割り当て機能の実装（要件保留中）
  - チーム内で議論し以下の確定が必要：対象者（駐車場と同じ10名？別メンバー？）・試合日の出欠判定方法・画面およびLINE出力フォーマット・累計／ポインタの管理方針・試合以外（合宿・運動会等）の扱い
  - 要件確定後、計画→実装フェーズへ移行
- 月をまたぐタイミング（5/1 0:00 JST）で閲覧者画面が自動的に5月表示へ切り替わるか実運用で確認
- 5月の駐車場・ビデオ当番を新しい10名構成で割り当てし、LINEへアナウンス
---

## 現在の状態：全タスク完了 ✅（2026-04-27時点）

---

## ✅ 完了済み

### 2026-04-27：林さんを駐車場当番からも除外
- **変更内容**：林和憲（ラグビー経験者・指導専念）を駐車場当番候補からも除外
  - `COACH_ORDER` を11名→10名に（ビデオと完全に同じメンバー構成）
  - `EXCLUDED_COACHES` に林さんを追加
- **マイグレーション処理**（起動時1回限り・`migrateRemoveHayashi`）：
  - 林さんの駐車場累計回数を削除
  - 5月以降の割り当て結果（assignments）をクリア → 再アップロード可能な状態に
  - 駐車場ポインタを「4月の最後の確定済み当番者の次」に巻き戻す
  - 完了フラグ `srs_migration_remove_hayashi_v1` を立てて再実行を防止
- **テスト**：林さんは元々テストデータで常に△か×だったので期待値は変わらず通過
- **CLAUDE.md**：固定順リスト・期待出力・テスト表を10名前提に更新（旧記述の不整合も同時修正）
- **デプロイ済み**：mainにpush → GitHub Actionsで自動デプロイ完了

### 2026-04-27：スマホ公開ボタンの不具合修正（publishToGithub）
- **症状1**：「公開する」ボタン押下時に「data.json does not match {sha}」エラー
  - 原因：ブラウザキャッシュにより GitHub API から古い SHA が返り、PUT 時にズレていた
  - 修正：GET にキャッシュバスティングクエリ（`?_=timestamp`）と `cache: 'no-store'` を追加。
    SHA不一致（409 or "does not match"）を検知したら最新 SHA を取り直して1回自動リトライ
- **症状2**：上記修正後に「Failed to fetch」エラーに変化
  - 原因：追加した `Cache-Control` / `If-None-Match` ヘッダーが GitHub API の CORS プリフライトを通らなかった
  - 修正：これらのヘッダーを削除し、キャッシュ回避はクエリパラメータと `cache: 'no-store'` のみで実現
- **動作確認**：塚原さんのMacで「公開する」成功確認済み
- **デプロイ済み**：mainにpush → GitHub Actionsで自動デプロイ完了

### 2026-03-30〜31：基本実装
- プロジェクトセットアップ（Vite + React + TypeScript）
- コアライブラリ4本（constants / assignParking / parseCsv / storage）
- コンポーネント10本すべて実装
- CSS（ラグビー緑 + ゴールドのデザイン）
- ユニットテスト 3件全通過（アルゴリズムが確定版テストデータと完全一致）
- 本番ビルド成功
- ブラウザで閲覧者モード表示確認（コンソールエラーなし）

### 2026-03-31：土曜日が当番対象になるバグ修正（AdminView.tsx）
- **バグ修正**：CSVアップロード時、土曜日が「練習」扱いになり当番がアサインされていた
  - 原因：csvSchedule生成時に土曜日チェックが抜けていた（DEFAULT_SCHEDULEに土曜はないため fallback で 'practice' になっていた）
  - 修正：`d.dayOfWeek === '土'` なら `'off'` を自動設定する条件を追加
- GitHub Pagesに再デプロイ済み

### 2026-03-31：CSVパーサーのエンコーディング誤判定バグ修正（parseCsv.ts）
- **バグ修正**：UTF-8のCSVがShift_JISとして誤デコードされ「日程」が文字化けするバグを修正
  - 原因：`isShiftJIS()`が `0xE0〜0xFC` 範囲でチェックしていたが、UTF-8日本語バイト列も同範囲に含まれるため誤判定
  - 修正：Shift_JIS判定関数を廃止し、「UTF-8で読んで失敗したらShift_JISにフォールバック」方式に変更
- GitHub Pagesに再デプロイ済み

### 2026-03-31：管理者モードのバグ修正（AdminView.tsx）
- **バグ1修正**：同月2回目のCSVアップロード時に「当番を決定する」ボタンが消えるバグを修正
  - 原因：`displayAssignments = assignments || savedData` で savedData が confirmed=true だとボタン条件が false になっていた
  - 修正：出欠確認中は古い保存データを参照しないよう `confirmedAttendance ? assignments : (assignments || savedData)` に変更
  - 合わせてボタン条件を `assignments === null` に変更
- **バグ2修正**：保存済み結果の担当者を手動変更しても保存されないバグを修正
  - 原因：`handleResultUpdate` が `assignments === null` の場合に早期リターンしていた
  - 修正：`assignments || savedData` をベースに更新するよう変更

---

## ✅ 残りの作業 → すべて完了（2026-04-09確認）

- ~~管理者モードの動作確認~~ → ✅
- ~~GitHub Pagesデプロイ~~ → ✅
- ~~スマホで「まだ決まっていません」になるバグ~~ → ✅
- ~~当番候補コーチ・除外コーチの編集UI~~ → ✅
- ~~スマホ実機テスト~~ → ✅
- ~~練習時間の閲覧者向け表示確認~~ → ✅
- ~~同日複数行のUI表示~~ → ✅

### 2026-04-06：ビデオ撮影当番機能追加
- **新機能**：駐車場当番（11名）とビデオ当番（10名、林除外）の2系統自動割り当て
- **被り防止**：同日に同じ人が駐車場とビデオの両方にアサインされない
- **土曜ルール**：土曜は駐車場なし（ー表示）、ビデオのみアサイン
- **データ構造変更**：ポインタ・累計回数を駐車場/ビデオで独立管理（旧データは自動リセット）
- **LINE出力**：「駐車場▶○○さん / ビデオ▶○○さん」形式に変更
- **テスト**：10件全通過（駐車場3件+ビデオ被り防止2件+統合5件）
- **デプロイ済み**：https://t-kyosuke.github.io/parking-lot-duty/

### 2026-03-31：UIリデザイン（Dark Rugby テーマ）
- **デザイン第2弾**：強豪ラグビーチームサイト風ダークテーマに全面変更
- 漆黒背景 + スクールイエロー（#F0DC00）で強烈なコントラスト
- ヘッダーに斜めストライプ装飾・黄色ボーダーライン
- 月表示を5remの極太Barlow Condensed 900weightに
- 背景に微細グリッドテクスチャ
- TODAY行を黄色反転（全行が黄色に）
- スケジュールタイトルにダイヤモンド型黄色アイコン
- Toastを黄色地・黒字・ALL CAPSに

### 2026-03-31：UIリデザイン（Black & Gold テーマ）
- **デザイン全面刷新**：深緑ベースから吹田ラグビースクールのスクールカラー（黒×黄色）に変更
- SRS公式ロゴ画像をヘッダーに追加（2倍解像度）
- Barlow Condensed フォント導入（月表示・見出しの力強さ向上）
- コーチバッジ：黒地・黄文字のピル型
- TODAY バッジ：スクールイエロー + パルスアニメーション
- タブ：黒地・黄文字のセグメントコントロール
- ログインカード：黄色トップバー
- 次回ポインタ：スクールイエロー全面カード

### 2026-04-12：OGPタグ更新（LINE共有時の表示変更）
- **変更内容**：LINEでURL共有時に表示されるタイトル・説明文を更新
  - タイトル：「SRS 駐車場当番」→「SRS 駐車場＆ビデオ当番」
  - 説明文：「吹田ラグビースクール（SRS）小学4年 駐車場当番スケジュール」→「SRS51th 駐車場＆ビデオ当番スケジュール」
  - `<title>`タグ、`<meta name="description">`、`og:title`、`og:description` の4箇所を変更
  - 変更ファイル：`index.html`
- mainブランチにコミット&プッシュ → GitHub Actionsで自動デプロイ

### 2026-04-11：TODAYバッジ位置修正
- **バグ修正**：閲覧画面でTODAYバッジが名前バッジと重なって表示されていた
  - 修正：`position: absolute; right: 220px; top: 50%; transform: translateY(-50%)` に変更
  - スマホ用も `right: 170px` に調整
  - 変更ファイル：`src/App.css`（`.today-marker` のPC・タブレット・スマホの3箇所）
- mainブランチにコミット&プッシュ → GitHub Actionsで自動デプロイ

### ✅ 動作確認済み（2026-03-31）
- CSVアップロード（UTF-8・複数月入り）→ 選択月のみ処理される
- 土曜日の自動除外
- 4月・5月それぞれ正常に割り当て動作
- 2ポインタアルゴリズム（代役が毎回異なる・担当回数が均一）
- 月をまたいだポインタの引き継ぎ

---

## 📝 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-03-30 | プロジェクト作成、コアライブラリ実装、全コンポーネント実装 |
| 2026-03-31 | TypeScript型エラー修正（verbatimModuleSyntax対応）、ビルド成功確認 |
| 2026-03-31 | CLAUDE.md / PROGRESS.md 作成（Claude Code引き継ぎ用） |
| 2026-03-31 | 管理者モードのバグ2件修正（割り当てボタン消失・手動変更無効） |
| 2026-03-31 | GitHub Pagesデプロイ完了（https://t-kyosuke.github.io/parking-lot-duty/） |
| 2026-04-01 | 閲覧者画面にお知らせ文を追加（黒地・黄色強調デザイン） |
| 2026-04-01 | スマホ対応：GitHub API経由でデータ公開・取得する方式に変更（publishToGithub / fetchPublishedData） |
| 2026-04-01 | デプロイ手順を確立（.envのGITHUB_TOKENを使うgit remote set-url方式） |
| 2026-04-02 | OGPタグ追加（LINE共有時の表示改善） |
| 2026-04-02 | エクスポート/インポートの2ポインタ不整合修正（owed+searchFromを保存） |
| 2026-04-02 | AdminView.tsxのany型をAssignmentResult[]に修正 |
| 2026-04-02 | fetchPublishedDataをraw.githubusercontent.comに変更（APIレート制限対策） |
| 2026-04-06 | ビデオ撮影当番機能を追加（2系統割り当て・被り防止・土曜ルール） |
| 2026-04-06 | LINE出力を新フォーマットに変更（駐車場▶/ビデオ▶形式） |
| 2026-04-06 | ポインタ・累計回数を駐車場/ビデオで独立管理に変更 |
| 2026-04-06 | 閲覧画面のデータ参照をlocalStorage優先に修正 |
| 2026-04-06 | GitHub Pagesに再デプロイ |
| 2026-04-07 | ヘッダータイトルを「SRS 駐車場当番」→「SRS 駐車場・ビデオ当番」に変更（Header.tsx:19） |
| 2026-04-07 | mainブランチにコミット&プッシュ → GitHub Actionsで自動デプロイ完了・本番反映確認済み |
| 2026-04-08 | スマホ表示向けにお知らせ文の改行を調整（PublicView.tsx）→ ビルド&デプロイ済み |
| 2026-04-11 | TODAYバッジの位置修正：名前バッジと被らないよう右端→「駐車場▶」ラベルの少し左・上下中央に移動（App.css）→ ビルド&デプロイ済み |
| 2026-04-12 | OGPタグ更新：LINE共有時の表示を「SRS 駐車場＆ビデオ当番」「SRS51th 駐車場＆ビデオ当番スケジュール」に変更（index.html）→ ビルド&デプロイ済み |
| 2026-04-27 | 林さんを駐車場当番からも除外（COACH_ORDER 11→10名、EXCLUDED_COACHES追加）。1回限りのマイグレーション処理で5月以降のassignments・林さんの累計をリセットしポインタを4月終了時点に巻き戻し |
| 2026-04-27 | スマホ公開時の「data.json does not match SHA」エラー修正：GETにキャッシュバスティング+cache:no-store追加、SHA不一致時の自動リトライ実装 |
| 2026-04-27 | スマホ公開時の「Failed to fetch」エラー修正：追加カスタムヘッダーがCORSプリフライトに弾かれていたため削除 |
